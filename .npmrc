auto-install-peers=true
strict-peer-dependencies=false
# Keep Prisma engine downloads working behind workspaces
node-linker=hoisted
# Don't hard-fail script runs over the optional @scarf/scarf telemetry build
verify-deps-before-run=false
# Disable package age restriction — some AWS SDK packages are very recent
minimum-release-age=0
