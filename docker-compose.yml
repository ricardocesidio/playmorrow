services:
  postgres:
    image: postgres:16-alpine
    container_name: playmorrow-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: playmorrow
      POSTGRES_PASSWORD: playmorrow
      POSTGRES_DB: playmorrow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U playmorrow"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
