app = 'playmorrow-api-aged-mountain-9542'
primary_region = 'ams'

[build]
  dockerfile = 'apps/api/Dockerfile'

[http_service]
  internal_port = 4000
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  memory = '512mb'
  cpus = 1
