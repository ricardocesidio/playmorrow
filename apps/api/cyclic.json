{
  "name": "playmorrow-api",
  "version": "1.0.0",
  "description": "Playmorrow API",
  "main": "dist/main.js",
  "scripts": {
    "build": "npx prisma generate && npx tsc",
    "start": "node dist/main.js"
  },
  "engines": {
    "node": "22.x"
  }
}
